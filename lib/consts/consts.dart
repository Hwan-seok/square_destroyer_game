class AppConst {
  static const TARGET_SIZE = 50.0;
  static const TARGET_COUNT = 10;
  static const TARGET_SPEED = 3.0;

  static const BALL_RADIUS = 20.0;
  static const BALL_SPEED = 20.0;

  static const SCORE_FONT_SIZE = 30.0;
  static const SCORE_PER_MATCH = 100;

  static const TIME_LEFT_FONT_SIZE = 42.0;

  static const GAME_DURATION = Duration(seconds: 60);
  static const GAME_SET = Duration.zero;
}
